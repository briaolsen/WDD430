

Alias to Custom Events??
[srvElement]="serverElemenent" in html
@Input('srvElement'): element{type: string, name: string, content: string};


Binding to Custom Events
add $event to function in html to catch in ts and use
@Output()

components styles can be encapsulated
ViewEncapsulation.ShadowDom Emulated None

Use on elements to identify them as Local References
#whateverNameHere 

Add what's in between the tags of the component
<ng-content></ng-content>

